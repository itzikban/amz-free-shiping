/**
 * compression:status — print the resolved compression config as a table or JSON.
 *
 * Usage:
 *   babysitter compression:status [--json]
 */
export interface CompressionStatusOptions {
    json?: boolean;
    cwd?: string;
}
export declare function handleCompressionStatus(opts: CompressionStatusOptions): number;
//# sourceMappingURL=compressionStatus.d.ts.map