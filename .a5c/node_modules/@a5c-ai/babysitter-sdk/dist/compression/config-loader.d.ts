/**
 * config-loader — loads and merges CompressionConfig from multiple sources.
 *
 * Priority (highest wins):
 *   env vars  >  project config (.a5c/compression.config.json)
 *             >  user config (~/.a5c/compression.config.json)
 *             >  built-in defaults
 */
import type { CompressionConfig } from './config';
export declare const COMPRESSION_ENV_VARS: {
    readonly ENABLED: "BABYSITTER_COMPRESSION_ENABLED";
    readonly USER_PROMPT: "BABYSITTER_COMPRESSION_USER_PROMPT";
    readonly COMMANDS: "BABYSITTER_COMPRESSION_COMMANDS";
    readonly SDK_CONTEXT: "BABYSITTER_COMPRESSION_SDK_CONTEXT";
    readonly LIBRARY_CACHE: "BABYSITTER_COMPRESSION_LIBRARY_CACHE";
};
/**
 * Load the effective CompressionConfig by merging all sources.
 *
 * @param projectDir  Root of the project (directory that contains .a5c/).
 *                    Defaults to `process.cwd()`.
 */
export declare function loadCompressionConfig(projectDir?: string): CompressionConfig;
//# sourceMappingURL=config-loader.d.ts.map